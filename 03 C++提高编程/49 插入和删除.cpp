/*
	push_back(elem);	//尾部插入元素elem
	push_front(elem);	//开头插入元素elem
	pop_back();			//删除最后一个元素
	pop_front();		//删除开头一个元素
	insert(pos,elem);	//位置pos插入元素elem 返回新数据位置
	insert(pos,int count,ele);	//位置pos插入count个元素ele 无返回值
	insert(pos,beg,end)；		//位置pos插入【beg,end)区间数据 无返回值
	erase(pos);		//删除pos位置的元素 返回下一个数据的位置
	erase(beg,end); //删除[beg,end)的元素 返回下一个数据的位置
	clear();		//删除容器中所有元素
	remove(elem);	//删除容器中所有值为elem的元素
*/